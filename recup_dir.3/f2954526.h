lease note that this should still be treated as a string.
   //  The first uint16 must hold the length of string (in bytes).
   //
   //******************************************************************

